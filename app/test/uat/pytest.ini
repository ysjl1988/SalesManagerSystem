[pytest]
markers =
    playwright: Playwright UI tests
    uat: User acceptance tests

addopts =
    --tb=short
    --html=./uat_test_report.html
    --self-contained-html
    -v

# 设置基本URL
base_url = http://127.0.0.1:5000
